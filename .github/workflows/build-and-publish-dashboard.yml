name: Dashboard CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  pull-requests: write
  id-token: write # This is required for requesting the JWT
  contents: read # This is required for actions/checkout

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
          run:
            working-directory: ./dashboard
    steps:
      - uses: actions/checkout@v4
       
      - name: Setup Node.
        uses: actions/setup-node@v4
        with:
          node-version-file: dashboard/.nvmrc
          cache: 'npm'
          
  #     - name: Install dependencies
  #       run: npm ci
        
  #     - name: Run tests
  #       run: npm run test
        
  # test-e2e:
  #   runs-on: ubuntu-latest
  #   needs: test
  #   steps:
  #     - uses: actions/checkout@v4
      
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version-file: '.nvmrc'
  #         cache: 'npm'
          
  #     - name: Install dependencies
  #       run: npm ci
        
  #     - name: Get stubs
  #       run: npm run getStubs
        
  #     - name: Build for e2e
  #       run: npm run build:ci
        
  #     - name: Install e2e dependencies
  #       run: npm run e2e:install
        
  #     - name: Run e2e tests
  #       run: npm run test:e2e:built
        
  #     - name: Upload e2e videos
  #       if: always()
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: e2e-videos
  #         path: e2e/videos/
          
  # build-and-publish-ui:
  #   runs-on: ubuntu-latest
  #   needs: [test, test-e2e]
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   steps:
  #     - uses: actions/checkout@v4
      
  #     - name: Configure AWS credentials
  #       uses: aws-actions/configure-aws-credentials@v4
  #       with:
  #         aws-region: eu-west-2
  #         role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
  #         role-duration-seconds: 1200
          
  #     - name: Login to Amazon ECR
  #       id: login-ecr
  #       uses: aws-actions/amazon-ecr-login@v2
        
  #     - name: Get ECR repository URI
  #       id: ecr-repo
  #       run: |
  #         echo "uri=$(aws ecr describe-repositories --repository-names prm-gp2gp-dashboard --query 'repositories[0].repositoryUri' --output text)" >> $GITHUB_OUTPUT
          
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version-file: '.nvmrc'
  #         cache: 'npm'
          
  #     - name: Install dependencies
  #       run: npm ci
        
  # - name: Build and push Docker image
  #   env:
  #     ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
  #     ECR_REPOSITORY: prm-gp2gp-dashboard
  #     IMAGE_TAG: ${{ github.sha }}
  #   run: |
  #     docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG -t $ECR_REGISTRY/$ECR_REPOSITORY:latest --build-arg BUILD_TAG=$IMAGE_TAG .
  #     docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
  #     docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest 